language: generic 
sudo: required
services :
  - docker

before install:
  - docker build -t dakshbhardwaj/docker-react -f Dockerfile.dev .

script:
  - docker run dakshbhardwaj/docker-react npm run test -- --coverage